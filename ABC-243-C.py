N=int(input())
X,Y=map(int,input().split() for i in range(N)
S=input() for i in range(N)

for i in range():
        for j in range():
        if S[i]==R:
        X+=1
        else:
        X-=1
        
        # (X[i],Y[j])についてはY軸の移動はないため、X軸のみをよく考える。
